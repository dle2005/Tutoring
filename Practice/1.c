/*
#include <stdio.h>

void main() {
	int n, sum = 0;

	while (1) {
		printf("0보다 큰 정수값 N을 입력하세요: ");
		scanf("%d", &n);

		if (n > 0) break;
		else printf("잘못입력하셨습니다. 다시 입력하세요\n");
	}

	for (int i = 0; i <= n; i++)
		sum += i;

	printf("1부터 %d까지의 합: %d\n", n, sum);
}
*/