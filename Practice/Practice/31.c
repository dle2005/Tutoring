/*
#include <stdio.h>

typedef struct student {
	int studentId;
	char blodType[2];
	double vision;
} student;

void main() {
	student arr[2];

	for (int i = 0; i < 2; i++) {
		scanf("%d %s %lf", &arr[i].studentId, &arr[i].blodType, &arr[i].vision);
	}

	for (int i = 0; i < 2; i++) {
		printf("학번: %d, 혈액형: %s, 시력: %.1lf\n", arr[i].studentId, arr[i].blodType, arr[i].vision);
	}
}
*/